// Reserved — see SiteShell.tsx for the standard wrapper used by ported pages.
// This file is intentionally unused; port-time render uses inline <style> + <main
// dangerouslySetInnerHTML> patterns.
export {};
